<?php

use Krystal\Widget\GridView\GridViewWidget;
use Krystal\Form\Element;
use Cms\View\Icon;

defined('KRYSTAL') or die;

?>

<div class="btn-toolbar">
    <div class="btn-group">
      <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
        <i class="glyphicon glyphicon-list"></i> <?php $this->show('Menu'); ?> <span class="caret"></span>
      </button>
      <ul class="dropdown-menu">
         <li><a href="<?php echo $this->url('Quiz:Admin:Question@addAction', $categoryId); ?>"><i class="glyphicon glyphicon-file"></i> <?php $this->show('Add new question'); ?></a></li>
         <li><a href="<?php echo $this->url('Quiz:Admin:Config@indexAction'); ?>"><i class="glyphicon glyphicon-tasks"></i> <?php $this->show('Configuration'); ?></a></li>
         <li><a href="<?php echo $this->url('Quiz:Admin:Category@addAction'); ?>"><i class="glyphicon glyphicon-folder-open"></i> <?php $this->show('Add new category'); ?></a></li>
         <li><a href="<?php echo $this->url('Quiz:Admin:History@indexAction'); ?>"><i class="fa fa-history"></i> <?php $this->show('History'); ?></a></li>

         <li class="divider"></li>
         <li class="dropdown-header"><?php $this->show('Actions'); ?></li>

         <?php if(!empty($questions)): ?>
         <?php if ($config->sortByOrder()): ?>
         <li><a href="#" data-button="save-changes" data-url="<?php echo $this->url('Quiz:Admin:Question@tweakAction'); ?>"><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Save changes'); ?></a></li>
         <?php endif; ?>
         <li><a href="#" data-button="remove-selected" data-url="<?php echo $this->url('Quiz:Admin:Question@deleteAction', null); ?>"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>

         <?php else: ?>
         <?php if ($config->sortByOrder()): ?>
         <li class="disabled"><a><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Save changes'); ?></a></li>
         <?php endif; ?>
         <li class="disabled"><a><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>

         <?php endif; ?>
      </ul>
    </div>

    <a target="_blank" href="<?php echo $this->url('Quiz:Quiz@indexAction'); ?>" class="btn btn-info"><i class="fa fa-folder-open"></i> <?php $this->show('Open on site'); ?></a>
</div>

<form>
    <div class="row">
        <div class="col-lg-6">
            <?php $this->loadPartial('items-per-page', array('caption' => 'Questions')); ?>
            <div class="table-responsive">
                <?= $this->widget(new GridViewWidget($questions, array(
                    'batch' => true,
                    'pk' => 'id',
                    'tableClass' => 'table table-hover table-bordered table-striped table-condensed',
                    'columns' => array(
                        array(
                            'column' => 'id',
                            'label' => '#'
                        ),

                        array(
                            'column' => 'question'
                        ),

                        array(
                            'column' => 'order',
                            'hidden' => !$config->sortByOrder(),
                            'value' => function($question){
                                return Element::number(sprintf('order[%s]', $question->getId()), $question->getOrder(), array('class' => 'form-control', 'min' => '0'));
                            }
                        )
                    ),

                    'actions' => array(
                        'details' => function($question){
                            return Icon::details(
                                $this->url('Quiz:Admin:Answer@listAction', $question->getId()),
                                $this->translate('View answers')
                            );
                        },

                        'edit' => function($question){
                            return Icon::edit(
                                $this->url('Quiz:Admin:Question@editAction', $question->getId()),
                                $this->translate('Edit this question')
                            );
                        },

                        'remove' => function($question){
                            return Icon::remove(
                                $this->url('Advice:Admin:Advice@deleteAction', $question->getId()),
                                $this->translate('Remove this question')
                            );
                        }
                    )
                ))); ?>

                <?php if (empty($questions)): ?>
                <div class="center-block">
                    <h2 class="text-center text-muted"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('No questions'); ?></h2>
                </div>
                <?php endif; ?>
                
                <?php $this->loadPartial('paginator'); ?>
            </div>
        </div>
        
        <div class="col-lg-6">
            <div class="table-responsive">
                <h4><i class="glyphicon glyphicon-th-list"></i> <?php $this->show('Categories'); ?> (<?php echo count($categories); ?>)</h4>

                <div class="table-responsive">
                    <?= $this->widget(new GridViewWidget($categories, array(
                        'tableClass' => 'table table-hover table-bordered table-striped table-condensed',
                        'rowAttributes' => array(
                            'class' => function($category) use ($categoryId){
                                return $categoryId == $category->getId() ? 'info' : null;
                            }
                        ),

                        'columns' => array(
                            array(
                                'column' => 'id',
                                'label' => '#'
                            ),

                            array(
                                'column' => 'name',
                                'value' => function($category){
                                    return Element::link($category->getName(), $this->url('Quiz:Admin:Browser@categoryAction', $category->getId(), 1));
                                }
                            ),

                            array(
                                'column' => 'questions_count',
                                'label' => 'Amount of questions'
                            ),

                            array(
                                'column' => 'order'
                            )
                        ),

                        'actions' => array(
                            'edit' => function($category){
                                return Icon::edit(
                                    $this->url('Quiz:Admin:Category@editAction', $category->getId()),
                                    $this->translate('Edit this category')
                                );
                            },

                            'remove' => function($category){
                                return Icon::remove(
                                    $this->url('Quiz:Admin:Category@deleteAction', $category->getId()),
                                    $this->translate('Remove this category')
                                );
                            }
                        )
                    ))); ?>

                    <?php if (empty($categories)): ?>
                    <div class="center-block">
                        <h2 class="text-center text-muted"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('No categories'); ?></h2>
                    </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</form>
