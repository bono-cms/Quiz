<?php

use Krystal\Form\Element;
defined('KRYSTAL') or die;

?>

<form class="card" data-group="config" autocomplete="off">
    <div class="form-group mb-3">
        <label><?= $this->translate('Answer sorting method'); ?></label>
        <div>
            <?= Element::select('config[order_type]', $this->translator->translateArray($config->getSortingTypes()), $config->getSortingMethod(), array('class' => 'form-control')); ?>
        </div>
    </div>

    <div class="form-group mb-3">
        <label><?= $this->translate('When passed test per category'); ?></label>
        <div>
            <?= Element::select('config[continue]', $this->translator->translateArray($config->getContinueTypes()), $config->getContinue(), array('class' => 'form-control')); ?>
        </div>
    </div>

    <div class="form-group mb-3">
        <label><?= $this->translate('Forced limitation of answers per category'); ?></label>
        <div>
            <?= Element::number('config[limit]', $config->getLimit(), array('class' => 'form-control', 'min' => 0)); ?>
        </div>
    </div>

    <div class="actions">
        <button data-button="save" data-url="<?php echo $this->url('Quiz:Admin:Config@saveAction'); ?>" class="btn btn-primary"><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Save'); ?></button>
        <button data-button="cancel" data-url="<?php echo $this->url('Quiz:Admin:Browser@indexAction'); ?>" class="btn btn-default"><i class="glyphicon glyphicon-floppy-open"></i> <?php $this->show('Cancel'); ?></button>
    </div>
</form>